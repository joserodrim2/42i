# Local (non-Docker) development connection string.
# Points at the Docker Compose `db` service published on host port 5433.
DATABASE_URL="postgresql://taskuser:taskpass@localhost:5433/tasks?schema=public"
